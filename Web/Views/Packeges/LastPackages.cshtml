@model IEnumerable<DomainClass.Models.Packages>
  <section class="last-products">
    <header><h3> جدیدترین بسته ها </h3> <a href="/ArchivePackage?pageId=1"> مشاهده همه بسته ها </a></header>
    <article>

        @foreach (var item in Model)
        {
            @Html.Partial("_package", item)
        }

        <div class="clearfix"></div>
    </article>
</section>
