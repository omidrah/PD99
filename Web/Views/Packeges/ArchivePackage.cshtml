@model IEnumerable<DomainClass.Models.Packages>
@{
    ViewBag.Title = "آرشیو بسته ها";
}

<section class="last-products">
    <header><h3>  بسته ها </h3></header>
    <article>

        @foreach (var item in Model)
        {
            @Html.Partial("_Package", item)
        }

        <div class="clearfix"></div>
    </article>
</section>
