@model IEnumerable<DomainClass.Models.Dooreha>
  <section class="last-products">
    <header><h3> جدیدترین دوره های آنلاین </h3> <a href="/ArchiveOnline?pageId=1"> مشاهده همه دوره های آنلاین </a></header>
    <article>

        @foreach (var item in Model)
        {
            @Html.Partial("_dooreh", item)
        }

        <div class="clearfix"></div>
    </article>
</section>
